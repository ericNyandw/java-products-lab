global:
  scrape_interval: 15s # Prometheus aspire les données toutes les 15 secondes

scrape_configs:
  - job_name: 'jenkins'
    metrics_path: '/prometheus/' # Le chemin que tu viens de valider !
    static_configs:
      - targets: ['host.docker.internal:8080']
